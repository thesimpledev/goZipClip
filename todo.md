# ZipClip todo

- Get ZipClip approved as a fully Google-authorized app (OAuth
  verification plus the YouTube API audit), so it ships one shared
  OAuth client and anyone can download it and sign in with Google to
  upload their videos, no personal Google Cloud project needed. The
  way apps like Nexus Clips work. Verification also lifts the
  private-only lock on API uploads and removes the unverified-app
  warning screen. Scheduled after the next step.
